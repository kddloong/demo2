import { createStyles } from 'antd-style';

export const addMemberClassnames = createStyles(({ css }) => ({
  'background-color-form': css`
    &.circle-picker {
      margin-bottom: 24px !important;
    }
  `,
}));
